{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"lbg-pipelines","text":"<p>Welcome to the documentation for the DESC LBG topical team's pipelines and analyses.</p> <p>This repository hosts the pipelines, notebooks, and supporting code for the DESC Lyman-break galaxy (LBG) topical team's science program.</p>"},{"location":"#quick-links","title":"Quick links","text":"Getting Started Environment setup and first pipeline run Pipelines Description of the pipelines Notebooks Analysis notebooks index Data Data locations and conventions on NERSC Contributing How to add code, open PRs, and ask questions"},{"location":"contributing/","title":"Contributing","text":""},{"location":"contributing/#workflow","title":"Workflow","text":"<p>Direct pushes to <code>main</code> are disabled \u2014 all changes must go through a pull request.</p> <ol> <li>Open an issue to discuss a new pipeline, analysis, or bug before writing code. This will make it easier to keep everyone up to date on what folks are working on so we don't duplicate work.</li> <li>Create a branch from <code>main</code>:    <pre><code>git checkout -b your-name/brief-description\n</code></pre></li> <li>Make your changes. See the sections below for conventions.</li> <li>Open a pull request against <code>main</code>. Use the PR template to describe your changes.</li> <li>Request a review from at least one other team member before merging.</li> </ol>"},{"location":"contributing/#branch-naming","title":"Branch naming","text":"<p>Use the pattern <code>&lt;yourname&gt;/&lt;topic&gt;</code>, e.g.:</p> <pre><code>jfcrenshaw/lbg-selection-cuts\n</code></pre>"},{"location":"contributing/#pipeline-changes","title":"Pipeline changes","text":"<ul> <li>Place new pipelines under <code>configs/pipelines/&lt;name&gt;/</code> (a <code>pipeline.yml</code> and a <code>config.yml</code>).</li> <li>Add a new run config under <code>configs/runs/&lt;name&gt;.yml</code> for any new dataset or run combination.</li> <li>Update <code>docs/pipelines/index.md</code> if a new pipeline is added.</li> <li>If you add new shared reference files, put them in <code>configs/</code>.</li> </ul>"},{"location":"contributing/#notebook-conventions","title":"Notebook conventions","text":"<ul> <li>Strip outputs before committing (handled by the <code>nbstripout</code> pre-commit hook).</li> <li>Name notebooks descriptively, e.g. <code>&lt;topic&gt;_&lt;data_version&gt;.ipynb</code>.</li> </ul>"},{"location":"contributing/#python-code","title":"Python code","text":"<p>We use ruff for linting and formatting (enforced by pre-commit):</p> <pre><code>ruff check --fix .   # lint\nruff format .        # format\n</code></pre>"},{"location":"contributing/#documentation","title":"Documentation","text":"<p>The docs site is built from the <code>docs/</code> directory with MkDocs Material. To preview locally:</p> <pre><code>pip install mkdocs-material\nmkdocs serve\n</code></pre> <p>Updates to <code>docs/</code> on <code>main</code> are deployed automatically to GitHub Pages via the CI workflow.</p>"},{"location":"contributing/#questions","title":"Questions","text":"<p>Post in the LBG topical team Slack channel (<code>#desc-lbg</code>) or open a GitHub issue.</p>"},{"location":"data/","title":"Data","text":"<p>We will update this page with paths and descriptions of data sets as we start to use them.</p>"},{"location":"data/#what-lives-in-the-repository","title":"What lives in the repository","text":"<p>The repo tracks only small reference files: Large catalogs, maps, and pipeline outputs are not tracked in git. They live on NERSC filesystems.</p>"},{"location":"data/#filesystem-conventions","title":"Filesystem conventions","text":"<ul> <li>Inputs come from paths specified in catalog config files.</li> <li>Outputs go to <code>results/&lt;run_name&gt;/outputs</code>.</li> <li>Logs go to <code>results/&lt;run_name&gt;/logs/</code>.</li> <li>Temporary files (e.g., jackknife scratch) go to <code>$PSCRATCH</code>.</li> </ul> <p><code>results/*/</code> is not tracked in git.</p>"},{"location":"getting_started/","title":"Getting Started","text":""},{"location":"getting_started/#environment-setup","title":"Environment setup","text":"<p>First setup the <code>lbg-env</code> tool by adding the following to your <code>~/.bashrc.ext</code> (or <code>~/.zshrc.ext</code>):</p> <pre><code>source /global/common/software/m1727/groups/WLSS/LBG/lbg-env/scripts/setup_lbg_env.sh\n</code></pre> <p>Then either start a new shell or re-source your <code>~/.bashrc.ext</code>/<code>~/.zshrc.ext</code> to pick up the change. You now have access to the <code>lbg-env</code> tool.</p> <p>Typing <code>lbg</code> in the terminal now provides a shortcut to the LBG project directory, and typing <code>lbg-env</code> will activate the latest shared LBG TT python environment. You can type <code>lbg-env help</code> to see other options provided by this tool, or you can see the README in the lbg-env GitHub repo.</p>"},{"location":"getting_started/#running-a-pipeline","title":"Running a pipeline","text":"<p>The simplest way to run a pipeline is with <code>run_debug.sh</code>, which requests a short debug allocation automatically and runs the pipeline inside it:</p> <pre><code>bash scripts/run_debug.sh &lt;run&gt;\n</code></pre> <p>where <code>&lt;run&gt;</code> is the name of a file (without the <code>.yml</code> extension) under <code>configs/runs/</code>. The pipeline(s) to execute are specified by the <code>pipelines:</code> key inside that run config.</p> <p>See Pipelines for a full description of each pipeline and instructions for creating run configs.</p>"},{"location":"getting_started/#contributing","title":"Contributing","text":"<p>To contribute to this project, see the instructions in Contributing</p>"},{"location":"notebooks/","title":"Notebooks","text":""},{"location":"notebooks/#notebook-descriptions","title":"Notebook descriptions","text":"<p>-</p>"},{"location":"notebooks/#creating-a-new-notebook","title":"Creating a new notebook","text":""},{"location":"notebooks/#naming-convention","title":"Naming convention","text":"<p>Use descriptive names that indicate the data version and topic, such as <code>&lt;topic&gt;_&lt;data_version&gt;.ipynb</code>. For example,</p> <pre><code>notebooks/lbg_selection_dp2_mocks_v1.ipynb\nnotebooks/angular_power_spectrum_dp2_23p5.ipynb\n</code></pre> <p>If you create a number of notebooks to explore results of a specific run you should create a subdirectory to collect those notebooks.</p>"},{"location":"notebooks/#adding-a-description","title":"Adding a description","text":"<p>Make sure you add a brief description to the list at the top of the document.</p>"},{"location":"notebooks/#data-paths","title":"Data paths","text":"<p>Notebooks should read data using absolute NERSC paths or the <code>$LBG_DIR</code> environment variable. Do not hardcode <code>/global/homes/...</code> paths that only work for one user.</p>"},{"location":"notebooks/#clearing-outputs","title":"Clearing outputs","text":"<p>A pre-commit hook strips cell outputs before committing to git. This ensures the git history does not get too large by including rendered images or data.</p>"},{"location":"pipelines/","title":"Pipelines","text":"<p>Our pipelines use the TXPipe / ceci framework. Each pipeline is a YAML file that declares a directed acyclic graph (DAG) of stages; ceci resolves dependencies and runs them in order.</p>"},{"location":"pipelines/#directory-structure","title":"Directory structure","text":"<pre><code>configs/\n\u251c\u2500\u2500 fiducial_cosmology.yml      # shared \u039bCDM reference parameters\n\u251c\u2500\u2500 pipelines/\n\u2502   \u2514\u2500\u2500 template/\n\u2502       \u251c\u2500\u2500 pipeline.yml        # stage definitions\n\u2502       \u2514\u2500\u2500 config.yml          # stage hyperparameters\n\u251c\u2500\u2500 runs/\n\u2502   \u2514\u2500\u2500 template.yml            # starting point for a new run\n\u2514\u2500\u2500 sites/\n    \u251c\u2500\u2500 nersc-interactive.yml   # launcher settings for interactive alloc\n    \u2514\u2500\u2500 nersc-batch.yml         # launcher settings for sbatch jobs\n</code></pre> <p>A pipeline run is configured by two kinds of file:</p> <ul> <li>one or more pipeline directories under <code>configs/pipelines/</code>, each defining a stage DAG and per-stage parameters, and</li> <li>a run file under <code>configs/runs/</code> that names which pipeline(s) to run and specifies inputs, output locations, and run options.</li> </ul> <p>The pipeline configs are kept agnostic to the specific catalogs being analysed, so the same pipeline can be rerun on different inputs simply by writing a new run config. A run config can also compose multiple pipelines by listing them under the <code>pipelines:</code> key \u2014 their stages, modules, and per-stage configs are merged automatically.</p>"},{"location":"pipelines/#pipeline-configs","title":"Pipeline configs","text":"<p>List of pipelines</p> <p>Each file in <code>configs/pipelines/</code> defines one pipeline:</p> <pre><code># Template pipeline \u2014 copy this directory to pipelines/&lt;name&gt;/\n\n# Space-separated list of modules to import so ceci can find the stage classes.\n# Default is just our lbg_stages module, but another common one is txpipe\nmodules: lbg_stages\n\n# Config file for this pipeline\nconfig: configs/pipelines/template/config.yml\n\n# Define all the stages.\n# Ceci will use their inputs/outputs to figure out in which order to run them.\nstages:\n  - name: Stage1\n  - name: Stage2\n  # Add more stages here\n</code></pre>"},{"location":"pipelines/#run-configs","title":"Run configs","text":"<p>List of runs</p> <p>Each file in <code>configs/runs/</code> defines one run:</p> <pre><code># Template run config \u2014 copy to runs/&lt;name&gt;.yml and fill in the details.\n\nresume: False  # Whether to re-run stages whose outputs already exist\n\n# Pipeline(s) to run. Use a single name or a list.\npipelines: &lt;pipeline-name&gt;\n# pipelines:\n#   - pipeline1\n#   - pipeline2\n\n# Location where outputs are saved - REPLACE &lt;run&gt; with the name of the run!\noutput_dir: results/&lt;run&gt;/outputs\nlog_dir: results/&lt;run&gt;/logs\n\n# Inputs required to run the stages in the pipeline\ninputs:\n  fiducial_cosmology: configs/fiducial_cosmology.yml\n  # Add more inputs here\n\n# These will be run before and after the pipeline respectively\npre_script: \"\"\npost_script: \"\"\n</code></pre>"},{"location":"pipelines/#running-a-pipeline","title":"Running a pipeline","text":"<p>Three run scripts are provided in <code>scripts/</code>. Each takes a run name as its first argument; the pipeline(s) to execute are read from the <code>pipelines:</code> key in the run config. Any additional arguments are forwarded directly to <code>ceci</code>.</p> <p>Quick test (requests a 2-node debug allocation automatically):</p> <pre><code>bash scripts/run_debug.sh &lt;run&gt; [ceci-args...]\n</code></pre> <p>Interactive (use when you already have an <code>salloc</code> allocation):</p> <pre><code>bash scripts/run_interactive.sh &lt;run&gt; [ceci-args...]\n</code></pre> <p>Batch job (queues a Slurm job; you do not need to stay logged in):</p> <pre><code>sbatch scripts/run_batch.sh &lt;run&gt; [ceci-args...]\n</code></pre> <p>In all cases <code>&lt;run&gt;</code> is the name of a <code>.yml</code> file (without the extension) under <code>configs/runs/</code>.</p>"},{"location":"pipelines/#adding-a-new-pipeline","title":"Adding a new pipeline","text":"<ol> <li> <p>Copy the template directory and edit the stages and config:</p> <pre><code>cp -r configs/pipelines/template configs/pipelines/&lt;pipeline_name&gt;\n# edit configs/pipelines/&lt;pipeline_name&gt;/pipeline.yml and config.yml\n</code></pre> </li> <li> <p>Add an entry to the list of pipelines that documents the pipeline, including the science intent.</p> </li> </ol>"},{"location":"pipelines/#adding-a-new-run","title":"Adding a new run","text":"<ol> <li> <p>Choose a descriptive run name. Something like <code>&lt;pipeline&gt;_&lt;catalog&gt;</code> is a good option, e.g. <code>clustering_dp2_24p5</code>.</p> </li> <li> <p>Copy the run template and fill in the pipeline(s), inputs, and output paths:</p> <pre><code>cp configs/runs/template.yml configs/runs/&lt;run_name&gt;.yml\n# edit pipelines:, output_dir, log_dir, and inputs:\n</code></pre> </li> <li> <p>Add an entry to the list of runs that documents the run, including inputs, software versions, and science intent.</p> </li> </ol>"}]}